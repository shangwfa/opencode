Load a specialized skill when the task at hand matches one of the skills listed in the system prompt.

Use this tool to load the skill's instructions. Session skill resources are materialized in the code-agent filesystem and returned as file references instead of being injected into the conversation.

The skill name must match one of the skills listed in your system prompt.
